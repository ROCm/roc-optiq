# CodeQL configuration
# Exclude third-party and build outputs to reduce noise and improve performance
paths-ignore:
  - thirdparty/**
  - build/**
  - Installer/**
  - docs/**

queries:
  - uses: security-and-quality

# Filter out specific queries from the suite
query-filters:
  - exclude:
      id: cpp/irregular-enum-init

